# PDF Editor

ブラウザだけで動く PDF 編集エディタです。PDF を読み込み、テキスト・ハイライト・
図形・フリーハンド・画像・吹き出しを重ねて、編集済み PDF として書き出せます。
注釈だけでなく、ページの入れ替え・結合・トリミング、フォーム入力、文字認識 (OCR)、
コメントによるレビュー、電子署名まで、ひととおりの作業がこの中で完結します。

**PDF はサーバーへアップロードされません。** 読み込みから書き出しまで、すべての処理が
お使いのブラウザ内で完結します。バックエンドはありません。

---

## 画面構成

```text
┌──────────────────────────────────────────────────────────────────┐
│ ファイル名 │ 履歴 │ ページ送り │ 表示倍率 │        開く  PDF書き出し │
├────┬──────────┬────────────────────────────────┬─────────────────┤
│ ツ │ ページ    │                                │ プロパティ       │
│ ー │ [1]      │   ページ1                       │                 │
│ ル │ [2]      │   ページ2  ← 全ページを連続表示   │ 選択中の要素に   │
│ 　 │ [3]      │   ページ3                       │ 応じて中身が変化 │
└────┴──────────┴────────────────────────────────┴─────────────────┘
```

一般的な PDF ビューアと同じく **全ページを縦に連続表示** します。描画は各ページが
画面に入ったときだけ行われるので、ページ数の多い PDF でも開いた瞬間に固まりません。

---

## 機能

### 編集ツール

| ツール | ショートカット | 操作 |
| --- | --- | --- |
| 選択 | `V` | クリックで選択、ドラッグで移動、余白のドラッグで範囲選択 |
| テキスト | `T` | クリックした位置にその場で入力（日本語 IME 対応） |
| ハイライト | `H` | ドラッグ。**元PDFの文字の行に自動で吸着**し、乗算合成で文字が透ける |
| フリーハンド | `P` | ドラッグでなぞり書き。ペン入力なら**筆圧で線の太さが変わる** |
| 四角形 | `R` | ドラッグ。角丸・塗り・枠線を設定できる |
| 円 | `O` | ドラッグ |
| 矢印 | `A` | ドラッグ。先端の有無を切り替え可 |
| 吹き出し | `C` | ドラッグ。**引き出し線の先も掴んで動かせる**（FreeText + Callout 注釈） |
| 画像 | `I` | クリックして PNG / JPEG を配置 |
| 既存テキストを編集 | `E` | 元PDFの文字をなぞると、**白で覆って打ち直せる状態**になる |

### 直接操作

- ドラッグで移動（`Shift` で水平・垂直に固定、`Alt` でスナップ解除）
- 8 方向のハンドルでリサイズ（`Shift` で縦横比を保持）
- **回転ハンドルで要素を自由な角度に回す**（`Shift` で 15° 刻み）
- **他の要素やページ中央への自動スナップ**（ピンクの目安線が出る）
- 矢印は両端のハンドルで向きと長さを変更
- テキストはリサイズでフォントサイズが拡大縮小。折り返し幅を決めた場合は幅の変更になる
- 範囲選択・`Shift` クリックでの複数選択、まとめて移動・削除
- **整列（6方向）・等間隔配置・グループ化・ロック・重なり順の変更**
- 矢印キーで微調整（`Shift` で大きく移動）
- 描画中に `Shift` で正方形 / 正円 / 45度スナップ

### 文字まわり

- **太字**（Noto Sans JP Bold を別途埋め込み）・**疑似斜体**
- 左 / 中央 / 右揃え
- **自動折り返し**（日本語の禁則処理つき。句読点や閉じ括弧が行頭に来ない）
- 8〜200pt、任意の色、不透明度

#### 日本語組版

| 設定 | 内容 |
| --- | --- |
| **縦書き** | 文字を縦に並べ、`ー`（長音）や括弧など**寝かせるべき字だけ 90° 回転**させます |
| **文字詰め** | `、` `。` `「` などの約物を詰めて、間延びを抑えます |
| **ぶら下げ組** | 行末の句読点を版面の外へ**ぶら下げ**ます |

これらは画面表示と書き出しで**同じ計算 (`textLayout.ts`)** を使っているため、
プレビューどおりの結果が PDF に入ります。

### スタンプ・署名

選択中の要素に名前を付けて保存しておき、別のページや別の文書へワンクリックで
配置できます。手書きのサインや「社外秘」などの定型スタンプに使えます。
保存先はブラウザ内（localStorage）のみです。

### コメント（レビュー）

要素ごとにコメントと返信を書き込めます。読み終わったものは「解決済み」にできます。

書き出すと、PDF の**本物の注釈**になります。作成者 (`/T`)、日時 (`/M`)、本文
(`/Contents`) が入り、返信は元の注釈へ `/IRT`（応答先）で紐づくので、Acrobat の
コメント一覧でもスレッドとして読めます。解決済みは `/State` = `Completed` です。

### レイヤー

右パネルの「レイヤー」タブに、そのページの要素が重なり順で並びます。

- 表示 / 非表示の切り替え
- ロック（動かせなくする）
- 名前を付ける
- ドラッグで重なり順を入れ替え

### フォーム（AcroForm）

元 PDF に入力欄がある場合、「フォーム」タブで値を読み書きできます。書き出すときに
**日本語フォントで外観を作り直す**ので、入力した日本語が消えません。「入力内容を
固定する」を選ぶと、編集できない通常の内容として焼き込みます。

### 文字認識（OCR）

スキャンした PDF のように文字情報を持たないページでも、ヘッダーの「文字認識 (OCR)」
から文字を起こせます。認識結果は元 PDF の文字情報とまったく同じ形で扱われるので、
そのまま**検索・ハイライトの行吸着・既存テキストの書き換え**に使えます。

認識エンジン (Tesseract) と学習データはこのアプリ自身から配信しています。
画像も認識結果も外部へ送りません。

### 電子署名

書き出す PDF に PKCS#7（detached, `adbe.pkcs7.detached`）の電子署名を付けられます。

- その場で自己署名の証明書を作る
- 手持ちの `.p12` / `.pfx` を読み込む

署名欄は Widget 注釈として置かれ、`/ByteRange` は署名値を除いた実バイト範囲を
指します。鍵の処理もすべてブラウザ内です。

### 印刷

ヘッダーの「印刷」から、**書き出すのと同じ PDF** をそのまま印刷できます。
画面の見た目ではなく実際の出力を印刷するので、ずれません。

### ページ管理

ページ一覧から、回転・複製・削除・並べ替え（**ドラッグ&ドロップ**または上下ボタン）が
できます。ページを動かすと、そのページに載っている編集要素も一緒に付いていきます。

- **PDF の結合**: ヘッダーの「結合」から別の PDF を後ろにつなげます
- **ページの抽出**: プロパティ欄の「ページ抽出」で、表示中のページだけを書き出します
- **白紙ページの挿入**: A4 / A3 / B4 / B5 / レターなどから選んで空のページを足せます
- **用紙サイズの変更**: 既存ページを別のサイズに合わせて拡大縮小します
- **トリミング**: 範囲を選んで「選択範囲で切抜」。CropBox として書き出すので、
  元の内容は失われず、いつでも解除できます
- **しおり（アウトライン）**は結合・並べ替えのあともページに追従します

### 表示・検索

- 幅に合わせる / 全体表示 / 25%〜300%
- `Cmd/Ctrl + ホイール` でズーム
- ページ一覧の表示切り替え
- **文書内検索（`Cmd/Ctrl + F`）**: 元PDFの文字を検索し、ヒット箇所を光らせて巡回します

### 数値と色の扱い

- 位置とサイズの**単位を `%` / `mm` / `pt` から選べます**（設定は次回も残ります）
- 使った色は「最近の色」として残り、一覧から選び直せます
- **スポイト**で画面上の色を拾えます（対応ブラウザのみ）

### 自動保存と「最近の文書」

編集内容・元PDF・貼り付けた画像を IndexedDB へ自動保存します。うっかりタブを
閉じても、次に開いたときに「続きから再開」できます。

保存は**文書ごとに分かれている**ので、ヘッダーの「最近の文書」から前に開いた
ものを選び直せます。復帰しないことを選んでも保存は消えません。
同じ文書を複数のタブで開いた場合は、**あとから開いたタブが上書きしない**よう
保存を止めます（どちらのタブの内容も失わないため）。

保存先はブラウザ内のみです。

### キーボードショートカット

| キー | 動作 |
| --- | --- |
| `V` `T` `H` `P` `R` `O` `A` `C` `I` `E` | ツール切り替え |
| `Cmd/Ctrl + Z` / `Cmd/Ctrl + Shift + Z` | 元に戻す / やり直す |
| `Cmd/Ctrl + F` | 文書内を検索 |
| `Cmd/Ctrl + S` | PDF書き出し |
| `Cmd/Ctrl + A` | 表示中のページの要素を全選択 |
| `Cmd/Ctrl + C` / `Cmd/Ctrl + V` | コピー / 貼り付け |
| `Cmd/Ctrl + D` | 複製 |
| `Cmd/Ctrl + G` / `Cmd/Ctrl + Shift + G` | グループ化 / 解除 |
| `Cmd/Ctrl + ]` / `[` | 前面へ / 背面へ（`Shift` で最前面 / 最背面） |
| `Delete` / `Backspace` | 削除 |
| `Escape` | 選択解除・編集終了 |
| `Cmd/Ctrl + Enter` | テキスト入力を確定 |
| `Tab` / `Shift + Tab` | 次 / 前の要素を選ぶ |
| `Enter` | 選択中のテキストを編集する |
| 矢印キー | 微調整 |

### 書き出し

元 PDF に編集レイヤーを載せた新しい PDF を生成し、`<元のファイル名>-edited.pdf`
としてダウンロードします。書き出し方は 2 種類から選べます。

| モード | 内容 |
| --- | --- |
| **焼き込み** | ページの内容に直接描く。どのビューアでも確実に見える |
| **PDF注釈** | Square / Circle / Line / Ink / Highlight / FreeText / Stamp / Widget 注釈として書き出す。Acrobat などで**あとから選択・移動・削除できる** |

注釈モードでは外観ストリーム (`/AP`) も自前で生成しているため、外観を自動生成しない
ビューアでも正しく表示されます。外観の中身はページへ焼き込むときと**同じオペレータ列**
なので、どちらのモードでも見た目は変わりません。

書き出しは **Web Worker** で行うため、ページ数が多くても操作が固まりません
（Worker を使えない環境では自動的にメインスレッドへ切り替わります）。

### パスワード保護されたPDF

パスワードを入力すれば開いて注釈を付けられます。ただし pdf-lib は暗号化 PDF を
書き出せないため、書き出し時はページを画像として取り込み直します（**文字は選択
できなくなります**）。その旨は書き出し後に画面へ表示されます。

---

## 使用技術

| 用途 | 採用 |
| --- | --- |
| フレームワーク | Next.js 16 (App Router) / React 19 |
| 言語 | TypeScript |
| スタイル | Tailwind CSS v4 |
| PDF 表示 | pdfjs-dist (PDF.js) |
| PDF 生成 | pdf-lib |
| フォント埋め込み | fontkit（サブセット化） |
| 文字認識 | Tesseract.js（自ドメイン配信） |
| 電子署名 | node-forge（PKCS#7 / PKCS#12） |
| 日本語フォント | Noto Sans JP (SIL OFL 1.1) |

状態管理は React の state とカスタムフックのみです。ドラッグ・リサイズ・範囲選択も
Pointer Events による自前実装で、外部ライブラリは使っていません。

---

## セットアップ

必要環境: Node.js 20.9 以上

```bash
npm install
```

`postinstall` で PDF.js の worker・CMap・標準フォントが `node_modules` から
`public/pdfjs/` へ複製されます。

### 起動

```bash
npm run dev      # http://localhost:3000
```

### ビルド

```bash
npm run build
npm run start
```

### 検査

```bash
npm run lint       # ESLint
npm run typecheck  # tsc --noEmit
```

---

## 公開（GitHub Pages）

サーバー処理を一切持たないアプリなので、**静的サイトとしてそのまま配信できます**。
`main` へ push すると [`.github/workflows/deploy-pages.yml`](.github/workflows/deploy-pages.yml)
がビルドして公開します。

```bash
npm run build:pages   # out/ に静的書き出し（NEXT_PUBLIC_BASE_PATH=/pdf_edit）
```

### 初回だけ必要な設定

リポジトリの **Settings → Pages → Build and deployment → Source** を
**「GitHub Actions」** に切り替えてください。ここが「Deploy from a branch」の
ままだと、`index.html` が無いために **README.md が代わりに表示されます**。

### パス接頭辞の扱い

プロジェクトページは `https://<user>.github.io/pdf_edit/` のように
**下の階層**で配信されます。Next.js の `basePath` は `<Link>` や `_next/` の
URL は面倒を見てくれますが、コードへ直接書いた絶対パス（フォント・PDF.js の
worker・OCR の学習データ）までは書き換えません。

そこで [`src/lib/basePath.ts`](src/lib/basePath.ts) の `assetUrl()` を通して
組み立てています。`@font-face` も同じ理由で、素の CSS ではなく
[`layout.tsx`](src/app/layout.tsx) で生成しています。

`NEXT_PUBLIC_BASE_PATH` が空のときは静的書き出しに切り替わらないので、
`npm run build && npm run start` での動作確認や E2E は従来どおりです。

---

## PDF 処理の仕組み

### 表示と編集の分離

元 PDF の canvas を書き換えることはしません。PDF.js が描いた canvas の上に、
編集専用のレイヤーを重ねています。

```text
Page Container (position: relative)
 ├── <canvas>            PDF.js が描画した元ページ（読み取り専用）
 ├── <svg>               編集レイヤー
 │    ├── 要素           text / rect / ellipse / line / path / image
 │    ├── 当たり判定      細い線でも掴めるよう、見た目とは別に用意
 │    └── 選択枠とハンドル
 └── <textarea>          テキスト入力中だけ現れる
```

編集レイヤーを SVG にしているのは **位置精度のため** です。SVG の `<text>` は
`y` 座標がそのままベースラインになり、これは PDF の描画モデルと同じです。
HTML の `<div>` で文字を置くと、ベースライン位置がブラウザの `line-height` 解釈や
OS ごとのフォントメトリクス選択に左右され、環境によって位置がずれます。

SVG の `viewBox` は PDF ポイント単位にしてあります。ズームは SVG のスケールだけで
吸収されるため、**内部の座標値はズーム率に一切依存しません**。

### 編集データ

```ts
type TextElement = {
  id: string;
  type: "text";
  pageIndex: number;   // 表示中のページ順での位置
  x: number;           // 正規化X座標 (0〜1)
  y: number;           // 正規化Y座標 (0〜1)、上が 0
  text: string;
  fontSize: number;    // PDFポイント (1/72インチ)
  color: string;
  align: "left" | "center" | "right";
  opacity: number;
};
```

座標はピクセルではなく **ページ幅・高さに対する比率 (0〜1)** で保持します。
ウィンドウサイズを変えてもズーム率を変えても位置はずれません。線の太さや
フォントサイズは PDF ポイントで持つので、こちらもズームに依存しません。

画像の実体（バイト列）は編集履歴に載せず、`ImageAssetStore` が別に保持します。
Undo のたびに数 MB をコピーしないためです。要素側は `assetId` だけを持ちます。

Undo / Redo の対象は「ページ構成 + 全要素」をまとめた 1 つのオブジェクトなので、
要素の編集だけでなくページの削除・回転・並べ替えも元に戻せます。

---

## 座標変換の仕組み

3 つの座標系があります。

| 座標系 | 原点 | Y方向 | 単位 |
| --- | --- | --- | --- |
| 正規化座標 | ページ左上 | 下 | 比率 (0〜1) |
| ビュー座標 | ページ左上 | 下 | PDFポイント |
| PDF ユーザー空間 | ページ左下 | 上 | PDFポイント |

### 1. 正規化座標 → ビュー座標

```ts
viewX = x * viewWidth;
viewY = y * viewHeight;
```

`viewWidth / viewHeight` はページ回転を適用した後の寸法です。画面のピクセル数は
「ビュー座標 × ズーム率 × 96/72」でしかないため、独立した座標系としては扱いません。

### 2. テキスト: 上端 → ベースライン

`y` はテキストブロックの上端です。PDF の `drawText` はベースライン基準なので、
フォントのアセントぶん下げます。

```ts
baselineY = viewY + ascentRatio * fontSize;
```

`ascentRatio` は fontkit で実フォントから読んだ値（Noto Sans JP なら 1.16）です。
2 行目以降は `LINE_HEIGHT_FACTOR (1.25) × fontSize` ずつ下がります。この計算は
プレビューと書き出しで同じ関数（`src/lib/pdf/textLayout.ts`）を使うため、必ず一致します。

### 3. ビュー座標 → PDF ユーザー空間

Y の反転に加えて、ページ回転と CropBox の原点オフセットを考慮します。

```ts
// 回転 0° の場合
pdfX = cropBox.x + viewX;
pdfY = cropBox.y + (cropBox.height - viewY);
```

90° / 180° / 270° 回転したページでは軸の対応が入れ替わります
（`src/lib/pdf/coordinates.ts` の `viewPointToPdfPoint`）。MediaBox / CropBox の原点が
`(0, 0)` でない PDF でも正しい位置に描けるよう、CropBox のオフセットを加算しています。

### 4. 図形ごとの書き出し

| 要素 | pdf-lib の呼び出し | 補足 |
| --- | --- | --- |
| テキスト | `drawText` | 行揃えは行ごとに開始位置をずらして表現 |
| ハイライト | `drawRectangle` | `BlendMode.Multiply` で下の文字を透かす |
| 四角形 / 円 | `drawRectangle` / `drawEllipse` | 4 隅を変換して外接矩形を取る |
| 矢印 | `drawLine` + `drawSvgPath` | 先端の三角形はパスで描く |
| フリーハンド | `drawSvgPath` | 丸い線端・線結合を明示 |
| 画像 | `drawImage` | ページ回転に合わせて原点を補正 |

`drawSvgPath` は内部で `translate → rotate → scale(1, -1)` を適用します。つまり
`x=0, y=0, scale=1` で呼べば、パス上の点 `(px, py)` は PDF 座標 `(px, -py)` に落ちます。
そこで **自前で PDF 座標へ変換した点の Y を反転して** 書き出すことで、ページ回転を
含めた変換結果をそのまま使えるようにしています。

### 精度

自動確認スクリプトで、プレビューの実描画ピクセルと書き出した PDF の実描画ピクセルを
突き合わせています。**ズレはページ寸法の 0.2% 未満**（A4 で 1pt 前後）です。
回転ページ・CropBox オフセットありの PDF でも同じ精度です。

---

## 日本語対応

`pdf-lib` の標準 14 フォントは日本語を持たないため、Noto Sans JP を PDF へ
埋め込んでいます。フォントは `public/fonts/` に同梱し、ライセンス全文
（`OFL.txt`）も併せて配置しています。

### サブセット埋め込み

フォント全体は 5.3MB あり、そのまま埋め込むと出力 PDF が 3MB 増えます。実際に
使った文字だけを取り出す（サブセット化）ことで、**数KB程度**に抑えています。

ここには落とし穴があります。`@pdf-lib/fontkit`（v1系）のサブセット生成は CJK
フォントで壊れたグリフを出力し、多くの漢字・かなが欠落します。そのため
**fontkit v2 のサブセット生成器**を使い、`pdf-lib` が期待する
`subset.encodeStream()` との差分だけをアダプタで吸収しています
（`src/lib/pdf/font.ts` の `createFontkitAdapter`）。

万一サブセット生成に失敗した場合は、`@pdf-lib/fontkit` でフォント全体を埋め込む
経路へ自動的に切り替わります。出力は重くなりますが文字化けはしません。

### フォントの取得タイミング

5MB 超あるため、PDF を開いてエディタを表示した時点で初めて取得します。トップ
画面を開いただけではダウンロードしません。

---

## プライバシー

- PDF ファイルはサーバーへ送信されません
- バックエンド API はありません。アプリは静的なフロントエンドのみです
- ファイルの読み込み・編集・書き出しはすべてブラウザのメモリ上で行われます
  （`File` / `ArrayBuffer` / `Uint8Array` / `Blob`）
- 貼り付けた画像も同様で、Blob URL は破棄時に解放されます
- 書き出した PDF は Blob URL 経由でダウンロードされ、URL は使用後に解放されます
- 外部の解析サービスやトラッキングは含まれていません

---

## 現在の制限

- **元PDFの文字の「その場編集」は、白で覆って打ち直す方式です。** 元のフォントを
  引き継ぐわけではないため、字形や字送りは完全には一致しません。誤字の修正や
  日付の差し替えといった用途を想定しています
- スキャンされた（文字情報を持たない）PDF は、**OCR を実行してから**でないと
  文字の選択・検索・書き換えができません。認識精度は原稿の状態に左右されます
- パスワード保護された PDF は、書き出すとページが画像になります
  （pdf-lib が暗号化 PDF を書き出せないため）
- ページを並べ替え・複製・削除・結合しても**しおりは追従します**が、
  フォームの入力欄は元の並びの単一ファイルでのみ引き継がれます
- 斜体は疑似斜体（字送りを傾ける方式）です。日本語フォントに斜体字形が無いためです
- 電子署名は**署名の作成のみ**です。既存の署名の検証はできません。また自己署名の
  証明書は、そのままではビューアに「発行元不明」と表示されます
- 自動保存はこのブラウザ内だけです。別の端末やブラウザには引き継がれません
- 画面幅 1280px 以上での利用を想定しています

## 動作確認

検証用 PDF を生成し、実ブラウザ (Chromium) でアプリを操作して確認するスクリプトを
同梱しています。プレビューと書き出し結果の **実描画ピクセル** を突き合わせるため、
位置ズレを見逃しません。

```bash
npx playwright install chromium   # 初回のみ
npm run fixtures                  # 検証用PDFを生成
npm run build && npm run start    # 別のターミナルで起動
npm run e2e
```

第1引数にアプリの URL を渡せるので、**GitHub Pages と同じくパス接頭辞つきで
配信した静的書き出し**に対しても同じ検査を回せます
（`node scripts/e2e-check.mjs http://localhost:3113/pdf_edit ...`）。

確認している内容（全 111 項目）:

| 分類 | 項目 |
| --- | --- |
| 読み込み | 1ページ / 複数ページ / 日本語 / ドラッグ&ドロップ / パスワード保護 |
| テキスト | 追加・日本語入力・IME変換確定・太字・斜体・折り返し・禁則処理 |
| 日本語組版 | 縦書き（寝かせる字の判定）・文字詰め・ぶら下げ組 |
| 吹き出し | 引き出し線の追従・出力への反映 |
| 図形 | 四角形 / 円 / 矢印 / フリーハンド / ハイライトの描画と位置の一致 |
| 要素の回転 | プレビューと出力の一致（誤差 0.11% 未満） |
| ハイライト | 乗算合成で下の文字が読めること・**文字の行への吸着** |
| 既存テキスト | なぞって取り込み・白で覆って書き換え・出力への反映 |
| 画像 | 貼り付けと書き出しへの反映 |
| 直接操作 | 移動 / リサイズ / 微調整 / 複製 / 範囲選択 / まとめて削除 / Undo / Redo |
| 整列 | 上揃え / 等間隔配置 / グループ化 / ロック |
| スナップ | 目安線の表示・他要素の端への吸着 |
| スタイル | 最後に使った色の引き継ぎ・重なり順の変更 |
| ページ | 連続表示 / ページ送り / 複製 / 並べ替えと出力への反映 |
| 結合・抽出 | 別PDFの結合・選択ページだけの抽出 |
| 検索 | ヒット箇所の表示 |
| 書き出し | 焼き込み / **PDF注釈**（注釈オブジェクトと外観の生成） |
| スタンプ | 保存と再配置 |
| 自動保存 | 再読み込み後の復帰・**最近の文書**からの開き直し |
| レイヤー | 一覧表示・表示/非表示・重なり順の入れ替え |
| コメント | 追加・返信・**PDF注釈 (`/Contents` `/IRT`) への書き出し** |
| 単位と色 | `mm` への切り替えと換算・最近使った色 |
| ページ生成 | 白紙ページの挿入・用紙サイズの変更（A3 = 842×1191pt） |
| 電子署名 | 署名つきPDFの生成・`/ByteRange` の妥当性・署名欄の注釈 |
| 印刷 | 印刷用 PDF の生成 |
| キーボード | `Tab` での要素巡回・`Enter` での編集開始 |
| ペン入力 | 筆圧の反映・手のひら誤爆よけ |
| 特殊なPDF | 回転ページ / CropBoxオフセット |
| エラー | PDF以外 / 壊れたPDF / パスワード誤り |
| 品質 | consoleエラー・未捕捉例外が出ないこと |

暗号化された検証用 PDF は `scripts/make-encrypted-fixture.mjs` が生成します
（pdf-lib は暗号化 PDF を書けないため、RC4 40bit の標準セキュリティハンドラを
素の PDF 構文で組み立てています）。

---

## ファイル構成

```text
src/
  app/
    layout.tsx / page.tsx / globals.css     @font-face の定義を含む

  components/editor/
    PdfEditor.tsx               全体の状態を持つ司令塔
    DropZone.tsx                初期画面
    AppHeader.tsx               ファイル操作・履歴・倍率・検索・書き出し方
    ToolRail.tsx                左端の縦型ツールバー
    PageRail.tsx                ページ一覧（ドラッグ並べ替え）
    PageThumbnail.tsx           遅延描画されるサムネイル
    DocumentView.tsx            全ページを縦に並べるスクロール領域
    PdfPageView.tsx             canvas + 編集レイヤー（描画は遅延・解放つき）
    ElementView.tsx             要素の SVG 描画
    SelectionLayer.tsx          選択枠・リサイズ/回転ハンドル
    InlineTextEditor.tsx        その場入力用の textarea (IME 対応)
    Inspector.tsx               選択中の要素に応じたプロパティ欄
    SidePanel.tsx               右パネルのタブ切り替え
    LayersPanel.tsx             重なり順・表示/非表示・ロック
    CommentsPanel.tsx           コメントと返信
    FormPanel.tsx               元PDFの入力欄
    SearchBar.tsx               文書内検索
    Dialogs.tsx                 パスワード入力・復帰確認・名前入力・電子署名・OCR
    StatusBanner.tsx            通知・エラー表示
    Icons.tsx                   アイコン

  hooks/
    useEditorHistory.ts         Undo / Redo
    usePageInteraction.ts       ページ上のポインタ操作（描画・移動・回転・範囲選択）
    usePdfDocument.ts           ファイル読み込み・パスワード・PDF.js の後始末
    useFontBook.ts              ウェイト別の日本語フォント
    useAutosave.ts              IndexedDB への自動保存

  lib/
    basePath.ts                 配信元のパス接頭辞の解決

  lib/pdf/
    renderPdf.ts                PDF.js の初期化と描画
    exportPdf.ts                書き出し（焼き込み / 注釈 / 画像フォールバック）
    exportRunner.ts             書き出しの Worker 実行（失敗時は主スレッドへ）
    drawElements.ts             要素 → PDF オペレータ（プレビューと共通の形）
    annotations.ts              PDF 注釈オブジェクトと外観ストリーム
    coordinates.ts              座標変換
    textLayout.ts               ベースライン計算・折り返し・禁則処理
    textLayer.ts                元PDFの文字位置の取り出し・検索
    geometry.ts                 外接矩形・リサイズ・回転・整列・スナップ
    elementDefaults.ts          要素の生成と既定スタイルの引き継ぎ
    imageAssets.ts              貼り付けた画像の保管
    stamps.ts                   スタンプ・署名の保存
    outlines.ts                 しおりの引き継ぎ
    forms.ts                    AcroForm の読み書きと固定化
    ocr.ts                      文字認識 (Tesseract)
    signature.ts                電子署名 (PKCS#7 / PKCS#12)
    units.ts                    % / mm / pt の相互変換
    persistence.ts              自動保存 (IndexedDB・文書ごと・タブ排他)
    font.ts                     フォント読み込みと fontkit アダプタ
    errors.ts                   日本語エラーメッセージへの変換

  workers/
    exportWorker.ts             書き出し専用の Web Worker

  types/
    editor.ts

public/
  fonts/                        Noto Sans JP Regular / Bold + ライセンス
  pdfjs/                        PDF.js の worker / CMap (postinstall で生成)
  ocr/                          Tesseract の実行ファイルと学習データ (同上)

.github/workflows/
  deploy-pages.yml              GitHub Pages への公開

scripts/
  copy-pdfjs-assets.mjs         PDF.js の配布物を public へ複製
  make-fixtures.mjs             検証用PDFの生成
  make-encrypted-fixture.mjs    パスワード保護PDFの生成
  e2e-check.mjs                 ブラウザでの動作確認（83項目）
```

---

## 今後追加予定の機能

### Phase 3 — スキャンPDFへの対応

- ~~OCR（文字情報を持たない PDF の文字起こし）~~ → 実装済み
- 傾き補正・下地の除去
- 元フォントを推定して引き継ぐ「その場編集」

### Phase 4 — AI によるPDF編集

- 自然言語による編集命令
  - 「全ページにページ番号を追加」
  - 「この文章を簡潔にして」
  - 「このロゴを全ページ右上に追加」
- 文書要約
- PDF への質問応答

### その他

- ~~文字詰め（アキ調整）を含む本格的な日本語組版~~ → 実装済み
- ~~縦書き~~ → 実装済み
- ~~電子署名~~ → 実装済み
- 表の作成
- 複数ファイルのタブ切り替え
- 既存の電子署名の検証
- ページ番号・透かしの一括挿入

## ライセンス

同梱している Noto Sans JP は SIL Open Font License 1.1 で配布されています。
詳細は [`public/fonts/OFL.txt`](public/fonts/OFL.txt) および
[`public/fonts/README.md`](public/fonts/README.md) を参照してください。
